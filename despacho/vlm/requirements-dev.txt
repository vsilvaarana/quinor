# =============================================================================
# Dependencias solo de desarrollo y pruebas. No se instalan en produccion.
# =============================================================================
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
ruff==0.8.4
